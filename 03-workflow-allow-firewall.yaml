---
- name: Permit traffic through firewall
  hosts: all
  become: true
  tasks:
    - name: Task 3 - allow traffic in firewall
      vars:
        ansible_python_interpreter: "{{ ansible_playbook_python }}"
      ansible.posix.firewalld:
        service: "{{ item }}"
        state: enabled
        permanent: true
        immediate: true
      loop:
        - http
        - https
