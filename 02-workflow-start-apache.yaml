---
- name: Install Apache package 
  hosts: rhel8
  become: true
  tasks:
    - name: task 2 - enable & start apache service
      ansible.builtin.service:
        name: httpd
        state: started
        enabled: true
