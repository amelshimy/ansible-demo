---
- name: Enable & Startup Apache service 
  hosts: rhel8
  become: true
  tasks:
    - name: task 2 - enable & start apache service
      ansible.builtin.service:
        name: http
        state: started
        enabled: true
